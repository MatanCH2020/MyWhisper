faster-whisper>=1.0.3
sounddevice>=0.4.6
numpy>=1.26
pyperclip>=1.8.2
# Qt UI toolkit — native, correct right-to-left/bidi rendering for Hebrew + tray.
PySide6-Essentials>=6.6
# Offline Hebrew word-frequency lexicon, used to flag unknown/mis-transcribed words.
wordfreq>=3.0
# CUDA runtime libs live in requirements-cuda.txt — setup.ps1 installs them
# only when an NVIDIA GPU is detected (they are a ~2GB+ download).
